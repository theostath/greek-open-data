# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/

# Secrets & local data (committed schema lives elsewhere)
.env
data/

# Local scratch
prompts.txt
.claude/settings.local.json

# Security-review stamp: a sha that is only meaningful on the machine and checkout
# that produced it. Committing one would let a stale review satisfy the gate elsewhere.
.claude/.last-security-review
